# ==========================
# Environment Configuration
# ==========================

# Frontend port (host → container:80)
FRONTEND_PORT=3300

# Backend port (host → container:8000)
BACKEND_PORT=3301

# Backend internal URL (service name inside Docker network)
BACKEND_URL=http://backend:8000
