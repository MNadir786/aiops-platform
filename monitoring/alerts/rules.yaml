groups:
- name: example
  rules:
  - alert: HighErrorRate
    expr: rate(http_requests_total[5m]) > 0.05
    for: 5m
    labels:
      severity: page
    annotations:
      summary: High request error rate
